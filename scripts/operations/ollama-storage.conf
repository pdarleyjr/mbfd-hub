[Unit]
# The model path is a symlink into this filesystem. Without this dependency,
# Ollama can start before the disk is mounted and reject the dangling path.
RequiresMountsFor=/mnt/mbfd-storage
After=mnt-mbfd\x2dstorage.mount

[Service]
ExecStartPre=/usr/bin/test -d /srv/models/ollama
