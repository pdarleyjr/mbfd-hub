# MBFD Hub — Project Summary
**Last Updated**: 2026-03-05 17:30 EST  
**Status**: ✅ ALL SYSTEMS OPERATIONAL

---

## What Is MBFD Hub?

MBFD Hub is the Miami Beach Fire Department's internal operations platform — a fully containerized web application running on a dedicated VPS at `145.223.73.170`. It provides:
- **Daily operational forms** (apparatus checkout, inspections, defect reporting)
- **Station inventory management** (PIN-gated, threshold alerts, supply requests)
- **Capital project tracking** (over and under $25k)
- **Workgroup/evaluation management** (Eval Feedback Hub)
- **Training resource management**
- **Personnel, apparatus, and equipment administration**
- **Google Sheets auto-sync** (Fire Apparatus → Equipment Maintenance tab)

---

## Technology Stack

| Layer | Technology |
|---|---|
| Backend | Laravel 11.31 + PHP 8.2 |
| Admin UI | Filament v3.2 (three panels) |
| Frontend SPA | React 18 + TypeScript + Vite |
| Database | PostgreSQL 15 |
| Queue/Cache | Redis |
| WebSockets | Laravel Reverb |
| Container | Docker Compose on Ubuntu VPS |
| CDN/Tunnel | Cloudflare Tunnel (tunnel ID: 89429799-7028-4df2-870d-f2fb858a49d7) |
| Monitoring | Sentry |
| Integrations | Google Sheets API v4 (service account auth) |

---

## Filament Panels

| Panel | URL Path | Purpose | Auth |
|---|---|---|---|
| Logistics/Admin | `/admin` | Fleet, inventory, projects, personnel | super_admin, admin, logistics_admin, training_admin |
| Training | `/training` | Training resources, todos, external sources | training_admin, training_viewer |
| Workgroup | `/workgroups` | Eval Feedback Hub — workgroup evaluations | super_admin, admin, logistics_admin, workgroup_admin, workgroup_facilitator, workgroup_member |

---

## Public SPAs (No Auth Required)

| App | URL | Tech |
|---|---|---|
| MBFD Forms (Daily Checkout) | `/daily` | React 18 + TypeScript + Vite PWA |
| Pump Simulator (internal V2 hidden) | `/pump-simulator` | React 18 + Zustand + Framer Motion |

**Landing page Pump Panel card** points to external: `https://pdarleyjr.github.io/puc-sim-manual-ui/`

---

## Key Features Implemented

### Google Sheets Apparatus Sync (Complete — 2026-03-03)
- One-way auto-sync: Fire Apparatus admin page → `Equipment Maintenance` Google Sheet tab
- Target spreadsheet: `1u9MYILAkfEaMfNZnBujvB1J0J33Ha8TybWCd_mVMJC4` (sheetId: `1714038258`)
- Column mapping: A=Designation, B=Vehicle#, C=Status, D=Location, E=Comments, F=Reported
- Trigger: Eloquent observer fires on any apparatus save; stamps `reported_at`; dispatches `SyncApparatusToSheetJob`
- Manual recovery: `artisan apparatus:sync-sheet [--dry-run] [--force]`
- Admin UI: "Sync to Google Sheet" header action (feature-flagged)
- Credentials: service account JSON stored at `/root/secrets/` (outside repo)

### Fire Apparatus Page UI Refactor (Complete — 2026-03-03)
- **Location column**: Smart condensed display merging Station + Assignment + Current Location
- **Class column**: Hidden by default (data preserved, user-togglable)
- **Notes → Comments**: Column/field relabeled
- **Reported**: New auto-stamped datetime column updated on any record change

### Station Inventory V2 (Complete)
- PIN-gated station access (default PIN: 1234)
- 35 inventory items across 5 categories
- 50% PAR threshold alerts (green/yellow/red)
- Special Supply Request workflow
- Full audit trail (employee name + shift)
- Admin dashboard with low-stock badges

### Workgroup / Eval Feedback Hub (Complete — 2026-02-28)
- Third Filament panel at `/workgroups`
- Workgroup creation and management
- Evaluation template builder
- Candidate product management
- Session-based evaluation flows
- Scoring, ranking, and finalists
- Specialized exporters (scores, finalists, feedback, completion status)
- **Evaluations are fully editable** — users can revisit and modify any submitted evaluation

### Pump Simulator V2 (Complete — 2026-03-03, HIDDEN on landing)
- Internal React SPA at `/pump-simulator` (still accessible directly but not linked from landing)
- Landing page "Pump Panel" card links to external: `https://pdarleyjr.github.io/puc-sim-manual-ui/`
- **⚠️ No `@apply` in CSS** (iOS black-screen crash prevention)

### CSV / XLSX Export Feature (Complete — 2026-03-03)
- Package: `pxlrbt/filament-excel ^2.5`
- All 43 Filament resource/relation manager tables have Export and Bulk Export buttons

---

## Infrastructure

### VPS
- **Host**: `145.223.73.170`
- **SSH**: `ssh -i "C:\Users\Peter Darley\.ssh\id_ed25519_hpb_docker" root@145.223.73.170`
- **App root**: `/root/mbfd-hub/`
- **Compose file**: `/root/mbfd-hub/compose.yaml`
- **Secrets**: `/root/secrets/google_service_account.json` (mounted read-only at `/run/secrets/`)

### Docker Services
| Service | Container Name | Notes |
|---|---|---|
| `laravel.test` | `mbfd-hub-laravel.test-1` | Laravel + Octane (port 8000) |
| `pgsql` | `mbfd-hub-pgsql-1` | PostgreSQL |
| `reverb` | — | WebSocket server (port 8080) |
| `baserow` | `baserow` | Self-hosted data platform |
| `nocobase` | `nocobase` | NocoBase CE (port 13000) |
| `cloudflared` | `cloudflared-mbfdhub` | Cloudflare Tunnel |

### Domains
- `www.mbfdhub.com` → Laravel app (via Cloudflare Tunnel)
- `nocobase.mbfdhub.com` → NocoBase CE
- `baserow.mbfdhub.com` → Baserow

### CI/CD
- GitHub repo: `pdarleyjr/mbfd-hub`
- Branch: `main`
- Workflow: `.github/workflows/deploy.yml`
- Smoke tests target `https://www.mbfdhub.com`

---

## Key File Locations

| Purpose | Path |
|---|---|
| Filament Admin resources | `app/Filament/Resources/` |
| Filament Training resources | `app/Filament/Training/Resources/` |
| Filament Workgroup resources | `app/Filament/Resources/Workgroup/` |
| Workgroup panel pages | `app/Filament/Workgroup/Pages/` |
| Workgroup exporters | `app/Filament/Workgroup/Exports/` |
| Export shared trait | `app/Filament/Concerns/HasExportActions.php` |
| React SPAs | `resources/js/daily-checkout/`, `resources/js/pump-simulator/` |
| Blade templates | `resources/views/` |
| Routes | `routes/web.php`, `routes/api.php` |
| Google Sheets sync | `app/Services/GoogleSheets/ApparatusSheetSyncService.php` |
| Apparatus observer | `app/Observers/ApparatusObserver.php` |
| Sync job | `app/Jobs/SyncApparatusToSheetJob.php` |
| Sync command | `app/Console/Commands/SyncApparatusSheet.php` |
| Sheets config | `config/google_sheets.php` |
| AI Context | `CLAUDE.md`, `MBFD_HUB_DISCOVERY_REPORT_2026-02-12.md` |

---

## Feature Flags

| Flag | Status | Notes |
|---|---|---|
| `GOOGLE_SHEETS_APPARATUS_SYNC_ENABLED` | `true` | Apparatus → Google Sheets auto-sync |
| `FEATURE_EMAIL_SENDING` | `false` | Gmail OAuth implemented but disabled |
| `FEATURE_REPLENISHMENT_DASHBOARD` | `false` | Feature built, pending go-live |

---

## ⚠️ Monitoring Notes

- **High-volume export risk**: `InspectionResource` and `StationInventorySubmissions` could exceed ~5,000 rows over time → migrate those to Filament's native queue-backed exporter if PHP memory issues arise during exports
- **NocoBase Phase 6**: Blocked on CE limitation (http-api datasource requires Pro). Workaround: `plugin-workflow-request` to call Baserow REST API internally
- **Redis Queue**: Database-backed queue via `QUEUE_CONNECTION=database` — monitor for job failures in Laravel Horizon or `artisan queue:work`
- **Google Sheets Sync**: If sync jobs fail repeatedly, check `/root/secrets/google_service_account.json` permissions and quota limits. Use `artisan apparatus:sync-sheet --force` for manual recovery.

---

## 🆕 Updates — 2026-03-04

### Workgroup Analytics Page (Admin Panel)
- **URL**: `/admin/workgroup-analytics`
- **Navigation**: Under "Workgroup Management" sidebar group
- **Features**:
  - AI Intelligence Summary card (connects to `mbfd-workgroup-ai.pdarleyjr.workers.dev`)
  - Active Session stats grid (session name, workgroup, submitted, drafts, products, avg score)
  - Top 3 Products Per Category with gold/silver/bronze visual ranking
  - Evaluator Completion Tracker table (submitted/missing/status per member)
- **Access**: `super_admin`, `admin`, `logistics_admin` roles

### Workgroup Data Hub (Workgroup Panel)
- **URL**: `/workgroups/admin-dashboard`
- Same analytics as the Admin panel version, accessible to workgroup facilitators/admins

### Session Results Page Fix
- **URL**: `/workgroups/session-results`
- Fixed 500 error (removed `parent::mount()`, fixed Filament v3 compatibility)
- Now accessible to ALL workgroup members (read-only)
- Shows aggregate product scores and anonymous feedback

### Note Sharing Feature
- Notes page now supports sharing notes with workgroup members
- Toggle to share with entire workgroup or specific member
- Share action on each note row
- Shared notes visible to recipients in their notes list
- `workgroup_notes` table has `is_shared` and `shared_with_user_id` columns

### Home Button Restoration
- Admin panel: Home button in header bar (before global search) + "Return to Home" in user menu
- Workgroup panel: `homeUrl('/')` + Home menu item in user dropdown
- Training panel: `homeUrl('/')` + Home menu item in user dropdown
- Pump simulator: Fixed red Home button overlay (mobile safe-area-aware)
- Daily checkout SPA: Sticky "Home" nav bar at top

### Cloudflare Workgroup AI Worker
- **Worker URL**: `https://mbfd-workgroup-ai.pdarleyjr.workers.dev`
- **Vectorize Index**: `workgroup-specs` (1024 dimensions, cosine metric)
- Endpoints: `/vectorize`, `/analyze`, `/summary`, `/executive-report`, `/health`
- Env var: `WORKGROUP_AI_WORKER_URL` in `.env`

### Pump Panel External Link
- Landing page "Pump Panel" card → `https://pdarleyjr.github.io/puc-sim-manual-ui/` (external, opens in new tab)
- Internal pump simulator (`/pump-simulator`) still exists but is not linked from landing

---

## 🐛 Critical Bug Fixes — 2026-03-05

### ⚠️ IMPORTANT FOR FUTURE AGENTS — DO NOT REPEAT THESE MISTAKES

#### Bug 1: Workgroup File Upload Crash
- **File**: `app/Filament/Workgroup/Pages/SharedUploads.php`
- **Error**: `Call to a member function store() on string`
- **Cause**: Filament's `FileUpload` component in Action forms returns a **string** (temporary Livewire file path), NOT an `UploadedFile` object. Calling `$file->store()` on a string crashes.
- **Fix**: Detect file type: if string, use `Storage::disk('local')->get()` then `Storage::disk('public')->put()` to move it. If `UploadedFile`, use standard `$file->store()`.
- **Pattern**: Always check `is_string($file)` vs `$file instanceof UploadedFile` when handling Filament FileUpload data.

#### Bug 2: Workgroup Panel 403 for Admin Users
- **File**: `app/Models/User.php` + `app/Http/Middleware/EnsureWorkgroupPanelAccess.php`
- **Error**: 403 Forbidden when admin users tried to access `/workgroups`
- **Cause**: `canAccessPanel('workgroups')` only allowed `super_admin`, `workgroup_*` roles — missing `admin` and `logistics_admin`.
- **Fix**: Added `admin` and `logistics_admin` to both `User::canAccessPanel()` and `EnsureWorkgroupPanelAccess` middleware.

#### Bug 3: Notes Page 500 Error for All Users
- **File**: `app/Models/WorkgroupNote.php`
- **Error**: `Argument #1 ($length) must be of type int, null given`
- **Cause**: `getPreviewAttribute(int $length = 100)` had `int` type hint. Laravel's Eloquent accessor system passes `null` as the first argument when calling accessors, which PHP rejects when typed `int`.
- **Fix**: Changed to `getPreviewAttribute($length = null)` and use `(int) ($length ?? 100)` inside.
- **Pattern**: NEVER use `int` type hint on Eloquent accessor methods — Eloquent passes `$value` (null for computed attributes) as first arg.

#### Bug 4: Evaluation Form 500 on Re-visit
- **File**: `app/Services/Workgroup/EvaluationService.php` → `getOrCreateDraft()`
- **Error**: `SQLSTATE[23505]: Unique violation: duplicate key value violates unique constraint "evaluation_submissions_workgroup_member_id_candidate_product_id"`
- **Cause**: `getOrCreateDraft()` filtered by `status = 'draft'`. When a user had already **submitted** (status = 'submitted'), it couldn't find the existing submission and tried to INSERT a new one, violating the unique constraint.
- **Fix**: Removed the `->where('status', 'draft')` filter so it finds ANY existing submission for the member+product combination.

#### Bug 5: Evaluation Form Read-Only Lock (User Request Fix)
- **File**: `app/Filament/Workgroup/Pages/EvaluationFormPage.php`
- **Issue**: Submitted evaluations were shown as read-only, users couldn't edit.
- **Fix**: Removed `$this->isReadOnly = true` from both the `loadProduct()` method and the `submitEvaluation()` method. Evaluations are now always editable.

---

## 🆕 Updates — 2026-03-05 (Evening)

### Workgroup Evaluation AI System (NEW — Separate from chatbot)
- **Worker**: `mbfd-workgroup-ai.pdarleyjr.workers.dev` (deployed via Wrangler)
- **Vectorize Index**: `workgroup-specs` (1024-dim, cosine) — vendor product PDFs/specs
- **Models**: `@cf/meta/llama-3.3-70b-instruct-fp8-fast` + `@cf/baai/bge-large-en-v1.5`
- **Endpoints**: `/vectorize`, `/analyze`, `/summary`, `/executive-report`, `/health`
- **Laravel integration**: `app/Services/Workgroup/WorkgroupAIService.php`
- **Auto-vectorize uploads**: `app/Observers/WorkgroupSharedUploadObserver.php`
- **API routes**: `POST /api/workgroup/ai/*`
- **AI Report Exporter**: `WorkgroupAIReportExporter` — exports products with AI analysis summaries
- **Export buttons**: "🤖 Export AI Report" in AdminDashboard + SessionResultsPage
- **AI Intelligence panel**: Alpine.js panel in both dashboard views for on-demand executive reports

### Landing Page AI Chatbot Optimizations
- Streaming responses with blinking cursor
- Conversation history (last 6 turns)  
- Clear conversation button
- Similarity threshold 0.2 (was 0.4 which was too strict)
- Temperature 0.3 for factual/procedural accuracy

### Bug Fixes (2026-03-05)
- SessionResultsPage `canAccess()` restored to allow ALL active workgroup members
- SessionResultsPage view fixed to use `getWidgets()` instead of `getHeaderWidgets()`
- Filament v3 incompatible `x-filament::card.heading` components fixed in admin-dashboard view

### Files Added/Modified
| File | Purpose |
|---|---|
| `cloudflare-worker/workgroup-ai/` | Workgroup AI Cloudflare Worker source |
| `cloudflare-worker/src/index.ts` | Chatbot worker optimized (threshold 0.2, streaming, history) |
| `app/Services/Workgroup/WorkgroupAIService.php` | Laravel AI service |
| `app/Filament/Workgroup/Exports/WorkgroupAIReportExporter.php` | AI-enhanced CSV/XLSX exporter |
| `app/Filament/Workgroup/Pages/AdminDashboard.php` | Added AI Report export + fixed Filament v3 view |
| `app/Filament/Workgroup/Pages/SessionResultsPage.php` | Fixed canAccess() for all members |
| `app/Observers/WorkgroupSharedUploadObserver.php` | Auto-vectorize uploaded files |
| `app/Http/Controllers/Workgroup/WorkgroupAIController.php` | API controller |
| `routes/api.php` | Added workgroup AI routes |
| `resources/views/welcome.blade.php` | Chatbot UI updates (streaming, history, clear) |
| `resources/views/filament-workgroup/pages/admin-dashboard.blade.php` | AI panel + Filament v3 fix |
| `resources/views/filament/workgroup/pages/session-results.blade.php` | AI panel + fixed widget rendering |
| `AI_AGENT_ERRORS.md` | Error log for future AI agents (local only, gitignored) |

---

## ⚠️ Monitoring Notes (Updated 2026-03-05)
- **Two Cloudflare Workers** — `mbfd-support-ai` (chatbot) and `mbfd-workgroup-ai` (eval AI) — keep SEPARATE
- **AI cache invalidation** — product analysis cache clears when eval submitted
- **Vector index** — `workgroup-specs` fills as files are uploaded; check Cloudflare dashboard for capacity
- **Chatbot threshold** — 0.2 similarity; if still returning "no info" responses, lower to 0.15

---

## GitHub Commits (2026-03-05)
- `93300b68` — fix: upload crash + workgroup panel access for admin/logistics_admin
- `d4924e37` — fix: WorkgroupNote preview accessor null length crash
- `554c9761` — fix: evaluation form 500 on re-visit (find submission regardless of status)
- `1c51e962` — feat: allow editing submitted evaluations
- `671a4ac3` — fix: 500 error on session-results (replace non-existent SelectAction)
- `da0268b7` — feat: enhanced SessionResultsPage with widgets, SAVER breakdown, AI report panel
- `66df0cc2` — fix: getHeaderWidgetsColumns/getFooterWidgetsColumns must be public
- `4fd153bc` — fix: register widgets in panel, fix 0% completion bug, auto-generate AI report

---

## 🆕 Session Results Page Rebuild — 2026-03-05 (Late Evening)

### Root Cause of 500 Error
`Filament\Actions\SelectAction` does not exist in Filament v3. The class was imported and used in `SessionResultsPage.php`, causing a fatal error on every page load.

### Bugs Fixed
1. **SelectAction doesn't exist** — Replaced with `Action` + `Select` form (Filament v3 pattern)
2. **FinalistsWidget uses BadgeColumn** — Replaced deprecated Filament v2 `BadgeColumn` with `TextColumn::badge()`
3. **0% completion bug** — `getSessionProgress()` had a chained query mutation bug: calling `.where('status','draft')` on a reused query builder permanently mutated it, making the subsequent `.where('status','submitted')` return 0 results
4. **Widget not registered in panel** — `FinalistsWidget` used in page but not registered in `WorkgroupPanelProvider->widgets()`, causing Livewire `ComponentNotFoundException` on AJAX updates
5. **heroicon-o-medal** — Non-existent icon replaced with `heroicon-o-trophy`

### Enhanced Features
- **Header Stats Widgets**: SessionProgressWidget showing session name, products, evaluators, progress %
- **Category Rankings Grid**: Full SAVER breakdown (S/A/V/E/R columns) per category with gold/silver badges
- **AI Executive Report**: Auto-generates on page load, with manual regenerate and copy-to-clipboard
- **Finalists Table Widget**: Filament v3-compatible table widget in footer
- **Session Switcher**: Action button with Select form to switch between sessions
- **Empty State**: Proper handling when no active session exists

### NotebookLM MCP Issue
The NotebookLM MCP server authentication was expired during this session. The `notebook_query` tool returned empty `answer: ""` silently. The `notebook_list` and `notebook_get` tools returned explicit auth errors. `refresh_auth` reported success but didn't fix it. Manual re-auth (`npx auth`) was needed but still returned empty answers. Context7 + local files were used as fallback for Filament v3 documentation research.
