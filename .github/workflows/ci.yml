name: CI

# This is validation only. It is deliberately unable to reach a self-hosted
# runner, deployment secret, production environment, or SSH target.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  release-gates:
    uses: ./.github/workflows/hub-release-gates.yml
